export const SF_API_VERSION = 'v39.0';

// Salesforce environment
const SANDBOX = 'https://test.salesforce.com';
const PRODUCTION = 'https://login.salesforce.com';
export const SF_ENV = {
  SANDBOX,
  PRODUCTION
};

const SOBJECTS = 'sobjects';
const SEARCH = 'search';
export const SF_API_TYPE = {
  SOBJECTS,
  SEARCH
};
